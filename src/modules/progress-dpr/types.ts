// Daily Progress Report: draft/submit/approve, quantities, photos, remarks (FR-030..035).
export interface DprHeader {
  id: string;
  reportDate: string;
  siteName: string;
  submittedBy: string;
  status: string;
}
